doctype html
html
    head
        title Express-app 3
        script(src='filer/index.js' defer)
    body
        input(type='textbox' id='navn')
        input(type='textbox' id='adresse')
        button OK
        ul 
            each person in personer
                li #{person.navn + ' ' + person.adresse}
