script.
    async function post(url, objekt) {
        const respons = await fetch(url, {
            method: "POST",
            body: JSON.stringify(objekt),
            headers: {
                'Content-Type': 'application/json'
            }
        });
        if (respons.status !== 201) // Created
            throw new Error(respons.status);
        return await respons.text();
    }

    let clickfunction = async () => {
            try {
                let tekst=document.querySelector("#tekst").value
                await post("http://localhost:8000/tilfoej", { tekst:tekst });
                window.location.href = "http://localhost:8000/index";
            } catch (e) {
                console.log("Fejl");
            }
        }

body  
    table
    div     
        ul
            each tekst in tekster 
                    li= tekst 
    p 
        |title:
    input(type='text', id="tekst" name='tekst', value='')
    button(onclick='clickfunction()')
        


